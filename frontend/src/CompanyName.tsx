import '@fontsource/roboto';
import { Typography } from '@mui/material';

const TechText: React.FC = () => (
    <Typography variant="h6" component="div" style={{ fontFamily: 'Roboto', fontWeight: 200, color: '#ffffff' }}>
        trustXchange
    </Typography>
);

export default TechText;